# Rote core runtime deps. Pinned for reproducibility.
# These four are small and offline-friendly (installable from a local package
# index / prefetched wheels). The optional local embedding backend lives in
# requirements-embed.txt and is NOT needed when OLLAMA_EMBED_URL points at an
# embed endpoint you own.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# sqlite-vec — loadable SQLite extension for vector similarity search. Ships
# precompiled binaries for the major platforms; loaded via sqlite_vec.load().
sqlite-vec==0.1.6
